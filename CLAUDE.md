@AGENTS.md

# 프로젝트 규칙 (CLAUDE.md)

이 문서는 이 저장소에서 Claude Code(및 다른 AI 코딩 도구)가 작업할 때 지켜야 할 규칙이다. 기획 배경과 상세 요구사항은 [PRD.md](PRD.md)를 참고한다.

## 프로젝트 개요

Jeisian 회사생활 도우미 — 직원이 인사·복지·오피스 환경·근무지원 제도에 대해 질문하면, 사내 규정 문서와 공지사항을 근거로 답하는 사내 챗봇.

## 기술 스택 (고정)

- **Next.js** (App Router, `src/` 디렉터리) — 화면과 API를 한 프로젝트에서 처리, PRD.md에서 정한 대로 고정
- TypeScript, Tailwind CSS, ESLint, npm
- 배포는 Vercel을 사용한다.
- 다른 프레임워크·언어·패키지 매니저로 바꾸거나 마이그레이션을 제안하지 않는다. 새 라이브러리가 필요하면 먼저 이유를 설명하고 사용자 승인을 받는다.

## 챗봇 응답 규칙 (PRD 5번 항목 반영)

챗봇 기능을 구현할 때는 다음 규칙을 코드/프롬프트에 반영한다.

1. **근거 없는 추측 금지**: 등록된 규정 문서·공지사항에서 확인되지 않는 내용은 답하지 않고 "확인되지 않음, 인사팀 문의 필요"로 응답한다. 답변에는 근거 문서명을 함께 표시한다.
2. **민감 사안은 반드시 인사팀으로 이관**: 확신도가 낮거나 개인별 판단이 필요한 사안(급여 이의제기, 징계 등)은 자체 판단하지 않고 인사팀 문의 채널을 안내한다.

## 범위 (PRD 6번 항목 반영)

- **만든다**: 규정 문서·공지사항 기반 질의응답 챗봇 (웹 채팅 UI)
- **만들지 않는다**: 연차 신청 등 실제 처리(액션), 개인별 인사정보(급여·평가·연차 잔여일수) 조회, 사내 시스템(전자결재/ERP) 연동, 모바일 앱, 다국어 지원
- 위 "만들지 않는다" 항목에 해당하는 기능을 요청받으면, 구현 전에 사용자에게 범위를 벗어난다는 점을 먼저 알린다.

## 보안 규칙

- API 키·토큰 등 비밀 값은 반드시 `.env`에 두고 코드에 하드코딩하지 않는다.
- `.env`, `node_modules`는 `.gitignore`에 등록해 두고, 절대 커밋하지 않는다. 커밋 전 항상 확인한다.
- 개인정보(급여, 평가, 개인 연차 잔여일수 등)를 다루는 코드나 목데이터를 추가하지 않는다.
- 외부 서비스 인증이 필요하면 토큰 값을 사용자에게 묻거나 채팅에 출력하지 말고, `.env`에 있는 값을 읽어서 사용한다.
  - 예: Supabase를 쓸 상황이 생기면 Supabase CLI를 설치해 `.env`의 `SUPABASE_ACCESS_TOKEN`으로 작업한다.
  - 예: Vercel 작업(배포 등)이 필요하면 Vercel CLI를 설치해 `.env`의 `VERCEL_TOKEN`으로 인증해 작업한다.

## 언어와 파일 위치

- 모든 설명과 주석은 한국어로 작성한다.
- 새 파일은 my-app 폴더 안에만 만든다.

## 작업 방식

- 기능 추가/변경 전 이 문서와 PRD.md의 범위를 벗어나지 않는지 먼저 확인한다.
- 큰 리팩터링이나 새 의존성 추가처럼 되돌리기 어려운 변경은 먼저 사용자에게 확인받는다.
- 코드를 바꾸면 반드시 무엇을 왜 바꿨는지 한 줄로 알려준다.
- 이미 설치된 서브에이전트는 필요할 때마다 적극 활용한다.

## 파일 삭제 규칙

- 파일을 지워야 할 때는 바로 삭제하지 말고, `trash-can` 폴더를 만들어 그 안으로 옮겨만 둔다.
- 최종 삭제는 사용자가 직접 확인한 뒤 진행한다.
